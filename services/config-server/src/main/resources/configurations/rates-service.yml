spring:
  cassandra:
   contact-points: host.docker.internal
   port: 9042
   keyspace-name: rates_keyspace
   schema-action: create_if_not_exists
   local-datacenter: datacenter1
   request:
    timeout: 10s


server:
  port: 7733