spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/rates
    username: maxvpire
    password: max26156
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

  flyway:
    enabled: true
    baseline-on-migrate: true
    baseline-description: "init"
    baseline-version: 0
    user: ${spring.datasource.username}
    password: ${spring.datasource.password}


server:
  port: 7733